{
  "name": "calendar",
  "version": "1.0.0",
  "main": "calendar.js",
  "license": "MIT",
  "scripts": {
    "dev": "while inotifywait -e close_write Calendar.html; do node build.js; done",
    "measure": "gzip -c calendar.js | wc -c"
  },
  "devDependencies": {
    "flatpickr": "^4.0.4",
    "pell": "^0.7.0",
    "svelte": "^1.40.2",
    "uglify-js": "^3.1.3"
  }
}
