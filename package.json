{
  "name": "calendar",
  "version": "1.0.0",
  "main": "calendar.js",
  "license": "MIT",
  "scripts": {
    "dev": "while inotifywait -e close_write Calendar.html Editor.html; do node build.js; done",
    "build": "NODE_ENV=production node build.js",
    "measure": "gzip -c calendar.js | wc -c"
  },
  "devDependencies": {
    "svelte": "^1.40.2",
    "uglify-js": "^3.1.3"
  }
}
